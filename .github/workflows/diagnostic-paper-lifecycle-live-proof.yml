name: Diagnostic Paper Lifecycle Live Proof

on:
  push:
    branches:
      - diagnostic/paper-lifecycle-live-proof

permissions:
  contents: read

jobs:
  live-paper-lifecycle-proof:
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
      - name: Read live E2E and strategy authority
        env:
          PRODUCTION_URL: https://solana-roi-convergence.onrender.com
          PYTHONUNBUFFERED: '1'
        run: |
          python - <<'PY'
          import json, os, urllib.request
          base = os.environ['PRODUCTION_URL'].rstrip('/')
          for path in ('/v1/strategy/e2e-status', '/v1/strategy/authority'):
              print(f'=== {path} ===', flush=True)
              request = urllib.request.Request(base + path, headers={'Accept':'application/json','User-Agent':'paper-entry-boundary-probe/1'})
              with urllib.request.urlopen(request, timeout=70) as response:
                  payload = json.loads(response.read().decode('utf-8'))
                  print(f'HTTP {response.status}', flush=True)
                  print(json.dumps(payload, sort_keys=True), flush=True)
          PY
